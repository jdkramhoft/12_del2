package logic;

import dal.DatabaseImplementation;
import dal.IWeightDAO;
import org.junit.After;
import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.*;

public class WeightSimTest {
    /*
    private ILogic sim;
    private IWeightDAO dao;
    private int invalidID = 1;


    //Hardcoded operator
    private int validID = 12;
    private String opName = "Anders And";

    //Hardcoded batch
    private

    @Before
    public void setUp(){
        this.dao = new DatabaseImplementation();
        this.sim = new WeightSim();
        sim.setDAO(dao);
    }

    @After
    public void tearDown(){
        this.dao = null;
        this.sim = null;
    }

    @Test
    public void shouldAuthorizeUser(){
        String name = sim.getUser(validID);
        assertEquals(opName, name);
    }

    @Test
    public void shouldNotAuthorizeUser(){
        String name = sim.getUser(invalidID);
        assertNull(name);
    }

    */

}